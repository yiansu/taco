// Generated by the Tensor Algebra Compiler (tensor-compiler.org)
// taco "y(i) = A(i, j) * x(j)" -f=y:d -f=A:ds -f=x:d -write-compute=compute.hpp

#if defined(USE_BASELINE) || defined(USE_FORKJOIN)
#include <functional>
#include <taskparts/benchmark.hpp>
#elif defined(USE_OPENMP)
#include "utility.hpp"
#else
#include "loop_handler.hpp"
#endif

#if defined(TEST_CORRECTNESS)
#include "taco.h"
using namespace taco;
#endif

#if defined(USE_BASELINE) || defined(TEST_CORRECTNESS)
void SPMV_baseline(
  double* __restrict__ y_vals,
  int A1_dimension,
  int* __restrict__ A2_pos,
  int* __restrict__ A2_crd,
  double* __restrict__ A_vals,
  double* __restrict__ x_vals
) {
  for (uint64_t i = 0; i < A1_dimension; i++) {
    double tjy_val = 0.0;
    for (uint64_t jA = A2_pos[i]; jA < A2_pos[(i + 1)]; jA++) {
      uint64_t j = A2_crd[jA];
      tjy_val += A_vals[jA] * x_vals[j];
    }
    y_vals[i] = tjy_val;
  }
}

#if defined(TEST_CORRECTNESS)
void test_correctness(
  int y1_dimension,
  double* __restrict__ y_vals,
  int A1_dimension,
  int* __restrict__ A2_pos,
  int* __restrict__ A2_crd,
  double* __restrict__ A_vals,
  double* __restrict__ x_vals
) {
  double* __restrict__ y_vals_ref = (double*)malloc(sizeof(double) * y1_dimension);

  SPMV_baseline(y_vals_ref, A1_dimension, A2_pos, A2_crd, A_vals, x_vals);

  uint64_t num_diffs = 0;
  double epsilon = 0.01;
  for (uint64_t i = 0; i < A1_dimension; i++) {
    auto diff = std::abs(y_vals[i] - y_vals_ref[i]);
    if (diff > epsilon) {
      printf("diff=%f y_vals[i]=%f y_vals_ref[i]=%f at i=%lu\n", diff, y_vals[i], y_vals_ref[i], i);
      num_diffs++;
    }
  }
  if (num_diffs > 0) {
    printf("\033[0;31mINCORRECT!\033[0m");
    printf("  num_diffs = %lu\n", num_diffs);
  } else {
    printf("\033[0;32mCORRECT!\033[0m\n");
  }
}

#endif
#endif

#if defined(USE_OPENMP)
void SPMV_openmp(
  double* __restrict__ y_vals,
  int A1_dimension,
  int* __restrict__ A2_pos,
  int* __restrict__ A2_crd,
  double* __restrict__ A_vals,
  double* __restrict__ x_vals
) {
#if defined(OMP_SCHEDULE_STATIC)
  #pragma omp parallel for schedule(static)
#elif defined(OMP_SCHEDULE_DYNAMIC)
  #pragma omp parallel for schedule(dynamic)
#elif defined(OMP_SCHEDULE_GUIDED)
  #pragma omp parallel for schedule(guided)
#endif
  for (uint64_t i = 0; i < A1_dimension; i++) {
    double tjy_val = 0.0;
    for (uint64_t jA = A2_pos[i]; jA < A2_pos[(i + 1)]; jA++) {
      uint64_t j = A2_crd[jA];
      tjy_val += A_vals[jA] * x_vals[j];
    }
    y_vals[i] = tjy_val;
  }
}

#elif defined(USE_HB_COMPILER)
#if defined(RUN_HEARTBEAT)
  bool run_heartbeat = true;
#else
  bool run_heartbeat = false;
#endif

double HEARTBEAT_loop1(
  uint64_t startIter,
  uint64_t maxIter,
  int* __restrict__ A2_crd,
  double* __restrict__ A_vals,
  double* __restrict__ x_vals
) {
  double tjy_val = 0.0;
  for (uint64_t jA = startIter; jA < maxIter; jA++) {
    uint64_t j = A2_crd[jA];
    tjy_val += A_vals[jA] * x_vals[j];
  }
  return tjy_val;
}

void HEARTBEAT_loop0(
  uint64_t maxIter,
  double* __restrict__ y_vals,
  int* __restrict__ A2_pos,
  int* __restrict__ A2_crd,
  double* __restrict__ A_vals,
  double* __restrict__ x_vals
) {
  for (uint64_t i = 0; i < maxIter; i++) {
    double tjy_val = 0.0;
    tjy_val += HEARTBEAT_loop1(A2_pos[i], A2_pos[(i + 1)], A2_crd, A_vals, x_vals);
    y_vals[i] = tjy_val;
  }
}

void SPMV_hbc(
  double* __restrict__ y_vals,
  int A1_dimension,
  int* __restrict__ A2_pos,
  int* __restrict__ A2_crd,
  double* __restrict__ A_vals,
  double* __restrict__ x_vals
) {
  HEARTBEAT_loop0(A1_dimension, y_vals, A2_pos, A2_crd, A_vals, x_vals);
}

#endif

int compute(taco_tensor_t *y, taco_tensor_t *A, taco_tensor_t *x) {
  auto runs = 10;
  if (const auto env_p = std::getenv("RUNS")) {
    if (const auto env_p_p = std::atol(env_p)) {
      runs = env_p_p;
    }
  }
  int y1_dimension = (int)(y->dimensions[0]);
  // double* __restrict__ y_vals = (double*)(y->vals);
  double* __restrict__ y_vals = (double*)malloc(runs * sizeof(double) * y1_dimension);
  int A1_dimension = (int)(A->dimensions[0]);
  int* __restrict__ A2_pos = (int*)(A->indices[1][0]);
  int* __restrict__ A2_crd = (int*)(A->indices[1][1]);
  double* __restrict__ A_vals = (double*)(A->vals);
  int x1_dimension = (int)(x->dimensions[0]);
  double* __restrict__ x_vals = (double*)(x->vals);

#if defined(USE_BASELINE) || defined(USE_FORKJOIN)
  taskparts::benchmark_nativeforkjoin([&] (auto sched) {
#elif defined(USE_OPENMP)
  utility::run([&] {
#elif defined(USE_HB_COMPILER)
  run_bench([&] {
#endif

    for (int i = 0; i < runs; i++) {
#if defined(USE_BASELINE)
      SPMV_baseline(y_vals + i * y1_dimension, A1_dimension, A2_pos, A2_crd, A_vals, x_vals);
#elif defined(USE_OPENMP)
      SPMV_openmp(y_vals + i * y1_dimension, A1_dimension, A2_pos, A2_crd, A_vals, x_vals);
#elif defined(USE_HB_COMPILER)
      SPMV_hbc(y_vals + i * y1_dimension, A1_dimension, A2_pos, A2_crd, A_vals, x_vals);
#endif
    }

#if defined(TEST_CORRECTNESS)
    test_correctness(y1_dimension, y_vals, A1_dimension, A2_pos, A2_crd, A_vals, x_vals);
#endif

#if defined(USE_BASELINE) || defined(USE_FORKJOIN)
  }, [&] (auto sched) {
  }, [&] (auto sched) {
  });
#else
  }, [&] {}, [&] {});
#endif

  return 0;
}
