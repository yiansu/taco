// Generated by the Tensor Algebra Compiler (tensor-compiler.org)
// taco "A(i, j) = B(i, j, k) * c(k)" -f=A:dd -f=B:dds -f=c:d -write-assembly=assemble2.hpp -write-compute=compute2.hpp

#if defined(USE_BASELINE) || defined(USE_FORKJOIN)
#include <functional>
#include <taskparts/benchmark.hpp>
#elif defined(USE_OPENMP)
#include "utility.hpp"
#else
#include "loop_handler.hpp"
#endif

#if defined(TEST_CORRECTNESS)
#include "taco.h"
using namespace taco;
#endif

#if defined(USE_BASELINE) || defined(TEST_CORRECTNESS)
void TTV_baseline(
  int A1_dimension,
  int A2_dimension,
  double* __restrict__ A_vals,
  int B1_dimension,
  int B2_dimension,
  int* __restrict__ B3_pos,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals
) {
  for (uint64_t i = 0; i < B1_dimension; i++) {
    for (uint64_t j = 0; j < B2_dimension; j++) {
      uint64_t jA = i * A2_dimension + j;
      uint64_t jB = i * B2_dimension + j;
      double tkA_val = 0.0;
      for (uint64_t kB = B3_pos[jB]; kB < B3_pos[(jB + 1)]; kB++) {
        uint64_t k = B3_crd[kB];
        tkA_val += B_vals[kB] * c_vals[k];
      }
      A_vals[jA] = tkA_val;
    }
  }
}

#if defined(TEST_CORRECTNESS)
void test_correctness(
  int A1_dimension,
  int A2_dimension,
  double* __restrict__ A_vals,
  int B1_dimension,
  int B2_dimension,
  int* __restrict__ B3_pos,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals
) {
  double* __restrict__ A_vals_ref = (double*)malloc(sizeof(double) * (A1_dimension * A2_dimension));

  TTV_baseline(A1_dimension, A2_dimension, A_vals_ref, B1_dimension, B2_dimension, B3_pos, B3_crd, B_vals, c_vals);

  uint64_t num_diffs = 0;
  double epsilon = 0.01;
  for (uint64_t i = 0; i < (A1_dimension * A2_dimension); i++) {
    auto diff = std::abs(A_vals[i] - A_vals_ref[i]);
    if (diff > epsilon) {
      printf("diff=%f A_vals[i]=%f A_vals_ref[i]=%f at i=%lu\n", diff, A_vals[i], A_vals_ref[i], i);
      num_diffs++;
    }
  }
  if (num_diffs > 0) {
    printf("\033[0;31mINCORRECT!\033[0m");
    printf("  num_diffs = %lu\n", num_diffs);
  } else {
    printf("\033[0;32mCORRECT!\033[0m\n");
  }
}

#endif
#endif

#if defined(USE_OPENMP)

#include <omp.h>

void TTV_openmp(
  int A1_dimension,
  int A2_dimension,
  double* __restrict__ A_vals,
  int B1_dimension,
  int B2_dimension,
  int* __restrict__ B3_pos,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals
) {
#if defined(OMP_NESTED_PARALLELISM)
  omp_set_max_active_levels(3);
#endif

#if !defined(OMP_CHUNKSIZE)
#if defined(OMP_SCHEDULE_STATIC)
  #pragma omp parallel for schedule(static)
#elif defined(OMP_SCHEDULE_DYNAMIC)
  #pragma omp parallel for schedule(dynamic)
#elif defined(OMP_SCHEDULE_GUIDED)
  #pragma omp parallel for schedule(guided)
#endif
#else
#if defined(OMP_SCHEDULE_STATIC)
  #pragma omp parallel for schedule(static, OMP_CHUNKSIZE)
#elif defined(OMP_SCHEDULE_DYNAMIC)
  #pragma omp parallel for schedule(dynamic, OMP_CHUNKSIZE)
#elif defined(OMP_SCHEDULE_GUIDED)
  #pragma omp parallel for schedule(guided, OMP_CHUNKSIZE)
#endif
#endif
  for (uint64_t i = 0; i < B1_dimension; i++) {
#if defined(OMP_NESTED_PARALLELISM)
#if !defined(OMP_CHUNKSIZE)
#if defined(OMP_SCHEDULE_STATIC)
    #pragma omp parallel for schedule(static)
#elif defined(OMP_SCHEDULE_DYNAMIC)
    #pragma omp parallel for schedule(dynamic)
#elif defined(OMP_SCHEDULE_GUIDED)
    #pragma omp parallel for schedule(guided)
#endif
#else
#if defined(OMP_SCHEDULE_STATIC)
    #pragma omp parallel for schedule(static, OMP_CHUNKSIZE)
#elif defined(OMP_SCHEDULE_DYNAMIC)
    #pragma omp parallel for schedule(dynamic, OMP_CHUNKSIZE)
#elif defined(OMP_SCHEDULE_GUIDED)
    #pragma omp parallel for schedule(guided, OMP_CHUNKSIZE)
#endif
#endif
#endif
    for (uint64_t j = 0; j < B2_dimension; j++) {
      uint64_t jA = i * A2_dimension + j;
      uint64_t jB = i * B2_dimension + j;
      double tkA_val = 0.0;
#if defined(OMP_NESTED_PARALLELISM)
#if !defined(OMP_CHUNKSIZE)
#if defined(OMP_SCHEDULE_STATIC)
      #pragma omp parallel for schedule(static) reduction(+:tkA_val)
#elif defined(OMP_SCHEDULE_DYNAMIC)
      #pragma omp parallel for schedule(dynamic) reduction(+:tkA_val)
#elif defined(OMP_SCHEDULE_GUIDED)
      #pragma omp parallel for schedule(guided) reduction(+:tkA_val)
#endif
#else
#if defined(OMP_SCHEDULE_STATIC)
      #pragma omp parallel for schedule(static, OMP_CHUNKSIZE) reduction(+:tkA_val)
#elif defined(OMP_SCHEDULE_DYNAMIC)
      #pragma omp parallel for schedule(dynamic, OMP_CHUNKSIZE) reduction(+:tkA_val)
#elif defined(OMP_SCHEDULE_GUIDED)
      #pragma omp parallel for schedule(guided, OMP_CHUNKSIZE) reduction(+:tkA_val)
#endif
#endif
#endif
      for (uint64_t kB = B3_pos[jB]; kB < B3_pos[(jB + 1)]; kB++) {
        uint64_t k = B3_crd[kB];
        tkA_val += B_vals[kB] * c_vals[k];
      }
      A_vals[jA] = tkA_val;
    }
  }
}

#elif defined(USE_HB_COMPILER)
#if defined(RUN_HEARTBEAT)
  bool run_heartbeat = true;
#else
  bool run_heartbeat = false;
#endif

double HEARTBEAT_loop2(
  uint64_t startIter,
  uint64_t maxIter,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals
) {
  double tkA_val = 0.0;
  for (uint64_t kB = startIter; kB < maxIter; kB++) {
    uint64_t k = B3_crd[kB];
    tkA_val += B_vals[kB] * c_vals[k];
  }
  return tkA_val;
}

void HEARTBEAT_loop1(
  uint64_t maxIter,
  int A2_dimension,
  double* __restrict__ A_vals,
  int B2_dimension,
  int* __restrict__ B3_pos,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals,
  uint64_t i
) {
  for (uint64_t j = 0; j < maxIter; j++) {
    uint64_t jA = i * A2_dimension + j;
    uint64_t jB = i * B2_dimension + j;
    double tkA_val = 0.0;
    tkA_val += HEARTBEAT_loop2(B3_pos[jB], B3_pos[(jB + 1)], B3_crd, B_vals, c_vals);
    A_vals[jA] = tkA_val;
  }
}

void HEARTBEAT_loop0(
  uint64_t maxIter,
  int A1_dimension,
  int A2_dimension,
  double* __restrict__ A_vals,
  int B2_dimension,
  int* __restrict__ B3_pos,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals
) {
  for (uint64_t i = 0; i < maxIter; i++) {
    HEARTBEAT_loop1(B2_dimension, A2_dimension, A_vals, B2_dimension, B3_pos, B3_crd, B_vals, c_vals, i);
  }
}

void TTV_hbc(
  int A1_dimension,
  int A2_dimension,
  double* __restrict__ A_vals,
  int B1_dimension,
  int B2_dimension,
  int* __restrict__ B3_pos,
  int* __restrict__ B3_crd,
  double* __restrict__ B_vals,
  double* __restrict__ c_vals
) {
  HEARTBEAT_loop0(B1_dimension, A1_dimension, A2_dimension, A_vals, B2_dimension, B3_pos, B3_crd, B_vals, c_vals);
}

#endif

int compute(taco_tensor_t *A, taco_tensor_t *B, taco_tensor_t *c) {
  auto runs = 10;
  if (const auto env_p = std::getenv("RUNS")) {
    if (const auto env_p_p = std::atol(env_p)) {
      runs = env_p_p;
    }
  }
  int A1_dimension = (int)(A->dimensions[0]);
  int A2_dimension = (int)(A->dimensions[1]);
  double* __restrict__ A_vals = (double*)(A->vals);
  int B1_dimension = (int)(B->dimensions[0]);
  int B2_dimension = (int)(B->dimensions[1]);
  int* __restrict__ B3_pos = (int*)(B->indices[2][0]);
  int* __restrict__ B3_crd = (int*)(B->indices[2][1]);
  double* __restrict__ B_vals = (double*)(B->vals);
  int c1_dimension = (int)(c->dimensions[0]);
  double* __restrict__ c_vals = (double*)(c->vals);

#if defined(USE_BASELINE) || defined(USE_FORKJOIN)
  taskparts::benchmark_nativeforkjoin([&] (auto sched) {
#elif defined(USE_OPENMP)
  utility::run([&] {
#elif defined(USE_HB_COMPILER)
  run_bench([&] {
#endif

    for (int i = 0; i < runs; i++) {
#if defined(USE_BASELINE)
      TTV_baseline(A1_dimension, A2_dimension, A_vals, B1_dimension, B2_dimension, B3_pos, B3_crd, B_vals, c_vals);
#elif defined(USE_OPENMP)
      TTV_openmp(A1_dimension, A2_dimension, A_vals, B1_dimension, B2_dimension, B3_pos, B3_crd, B_vals, c_vals);
#elif defined(USE_HB_COMPILER)
      TTV_hbc(A1_dimension, A2_dimension, A_vals, B1_dimension, B2_dimension, B3_pos, B3_crd, B_vals, c_vals);
#endif
    }

#if defined(TEST_CORRECTNESS)
    test_correctness(A1_dimension, A2_dimension, A_vals, B1_dimension, B2_dimension, B3_pos, B3_crd, B_vals, c_vals);
#endif

#if defined(USE_BASELINE) || defined(USE_FORKJOIN)
  }, [&] (auto sched) {
  }, [&] (auto sched) {
  });
#else
  }, [&] {}, [&] {});
#endif

  return 0;
}
